这份需求旨在在现有的 OctoPrint 逐行发送机制上，增加一个“中间人拦截与动态修改”层。

-----

# 任务需求文档：Gcode 实时速度覆写功能 (Real-time Feed Rate Override)

## 1\. 背景与目标

当前系统包含一个 Gcode 发送模块，能够读取本地 Gcode 文件并通过 API 逐行发送给 OctoPrint 以控制打印机。系统已具备基础的“暂停”和“恢复”发送功能。

**目标：** 新增一个“速度覆写（Speed Override）”功能。允许用户在打印过程中手动暂停发送，开启速度修改模式（设定新的速度倍率或固定值），然后恢复打印。此时发送给打印机的 Gcode 中的速度参数（F值）将被实时修改，而本地原始文件保持不变。用户也可以随时关闭此功能，恢复原始速度打印。

## 2\. 核心用户流程 (User Story)

1.  **正常打印：** 用户开始打印，系统按原文件发送指令。
2.  **触发修改：** 当打印机运行到特定位置（或用户观察到需要调整时），用户点击“暂停发送”。
3.  **配置速度：** 用户开启“速度覆写”开关，并输入目标速度（例如：将速度设为原来的 50% 或固定值 F1500）。
4.  **应用修改：** 用户点击“恢复发送”。
5.  **执行修改：** 系统读取下一行 Gcode，**在内存中**修改其 `F` 参数，然后发送给 OctoPrint。
6.  **取消修改：** 一段时间后，用户再次“暂停”，关闭“速度覆写”开关，点击“恢复”，系统继续发送原始 Gcode。

-----

## 3\. 功能需求 (Functional Requirements)

### 3.1 速度拦截与解析逻辑

  * **Gcode 解析：** 系统必须在发送指令前，利用正则或字符串分析检查当前行是否包含速度指令（即 `F` 参数，如 `G1 X10 Y10 F3000`）。
  * **动态计算：**
      * 如果检测到 `F` 参数且“速度覆写”功能已开启：
          * 提取原始 `F` 值。
          * 根据用户设定的规则（百分比或固定值）计算新 `F` 值。
          * **必须保留**该行中的其他坐标参数（X, Y, Z, E）。
      * 如果未检测到 `F` 参数：直接发送原指令。
  * **状态保持（Statefulness）：** 打印机通常是模态的（Modal）。如果一行指令修改了速度（如 `G1 F3000`），后续没有 `F` 的移动指令都会沿用这个速度。
      * *需求：* 当开启覆写模式时，系统需要向每一条移动指令（G0/G1）注入修改后的 `F` 值，或者确保修改后的速度在后续指令中持续生效。

### 3.2 控制模式 (UI/交互逻辑)

  * **覆写开关 (Toggle Switch)：** 一个全局开关，用于启用或禁用速度修改逻辑。
  * **速度设定方式 (Input Method)：** 建议提供两种模式（二选一或仅实现一种）：
    1.  **百分比缩放 (Scaling)：** 例如输入 `0.5` 或 `50%`，则 `New_F = Original_F * 0.5`。（推荐，通用性强）
    2.  **固定值 (Fixed Value)：** 例如输入 `1200`，则所有移动指令强制改为 `F1200`。（适用于需要强制慢速过弯的场景）
  * **无损原始文件：** 所有的修改仅发生在\*\*数据发送缓冲区（Buffer/Stream）\*\*中，**严禁**修改磁盘上的原始 `.gcode` 文件。

### 3.3 暂停与恢复的配合

  * **暂停逻辑：** 暂停时，仅仅停止向 OctoPrint 发送新的指令行。
  * **修改时机：** 允许用户在“暂停”状态下调整速度参数。
  * **恢复逻辑：** 恢复时，立即使用新的速度参数处理下一条待发送的指令。

-----

## 4\. 技术实现建议 (Technical Implementation)

### 4.1 伪代码逻辑 (Python 风格示例)

```python
class GcodeSender:
    def __init__(self):
        self.is_paused = False
        self.speed_override_enabled = False
        self.speed_factor = 1.0 # 1.0 表示 100% 原速
        self.current_gcode_line_index = 0
        self.gcode_lines = [] # 原始文件内容加载到内存

    def process_next_line(self):
        if self.is_paused:
            return

        raw_line = self.gcode_lines[self.current_gcode_line_index]
        
        # 核心逻辑：发送前处理
        line_to_send = raw_line
        if self.speed_override_enabled:
            line_to_send = self.modify_feedrate(raw_line, self.speed_factor)
            
        self.send_to_octoprint(line_to_send)
        self.current_gcode_line_index += 1

    def modify_feedrate(self, gcode_str, factor):
        # 使用正则查找 F 以及后面的数字
        # 如果找到，进行数学运算替换
        # 如果没找到 F，是否需要强制插入 F？(视具体需求而定)
        # 返回修改后的字符串
        pass
```

### 4.2 关键正则表达式

建议使用类似以下的正则来捕获 F 值：
`[Ff](\d+(\.\d+)?)`

  * 匹配 `F` 或 `f` 开头，后跟整数或浮点数。

-----

## 5\. 边界情况与风险控制 (Edge Cases)

  * **无 F 指令的处理：**
      * 如果当前行是 `G1 X10`（没有 F），打印机沿用之前的速度。
      * *风险：* 如果之前是高速，用户想变低速，但这一行没有 F，覆写可能不生效。
      * *解决方案：* 系统应维护一个 `last_known_F` 变量。开启覆写时，如果当前行没有 F，应强制追加 `F{last_known_F * factor}` 到指令末尾。
  * **非移动指令：** 确保不修改非移动指令（如 M104 温度控制, M106 风扇等）中的参数，防止误判。仅针对 `G0`, `G1`, `G2`, `G3` 指令进行 F 值检查。
  * **极低速度保护：** 防止计算出的速度过低（例如 F0 或负数），应设置最小速度阈值（例如 F60）。

-----

## 6\. 验收标准 (Acceptance Criteria)

1.  [ ] 在不开启开关时，打印过程与修改前完全一致。
2.  [ ] 点击暂停后，可以开启开关并设置速度倍率（如 50%）。
3.  [ ] 恢复打印后，OctoPrint 终端收到的指令中，F 值为原文件对应行 F 值的 50%。
4.  [ ] 再次暂停并关闭开关，恢复打印后，指令速度恢复正常。
5.  [ ] 检查本地磁盘上的 Gcode 文件，其 MD5 值/修改时间未发生变化。

-----